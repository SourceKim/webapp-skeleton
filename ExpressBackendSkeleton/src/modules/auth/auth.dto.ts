import { UserStatus, UserGender } from '@/modules/user/user.model';

// 所有 DTO 已迁移到 @skeleton/shared-types，请使用 type import
// 例如: import type { RegisterDto, LoginDto, RegisterResponseDto, LoginResponseDto, ProfileResponseDto, UpdateProfileDto } from '@skeleton/shared-types' 