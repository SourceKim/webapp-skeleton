# ============================================
# Skeleton 项目统一环境变量配置
# ============================================
# 
# 使用方法:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际情况修改配置值
# 3. 运行脚本时会自动加载 .env 文件中的配置
#
# ============================================

# --------------------------------------------
# 公共环境变量配置（所有项目共享）
# --------------------------------------------

# 日志级别配置
# 可选值: error, warn, info, http, debug
# 默认值: info
LOG_LEVEL=info

# 数据库日志开关（仅后端）
# 可选值: true, false
# 默认值: true（开发环境输出查询日志，生产环境只输出错误和警告）
DB_LOGGING=true

# 运行环境
# 可选值: development, production
# 默认值: development
NODE_ENV=development

# --------------------------------------------
# 部署路径配置
# --------------------------------------------

# VueAdminSkeleton (管理后台) 部署路径
# 示例: /opt/1panel/www/sites/skeleton-admin.kimsu.fun/index
DEPLOY_PATH_ADMIN=

# TaroFrontendSkeleton (Taro H5) 部署路径
# 示例: /opt/1panel/www/sites/skeleton-web.kimsu.fun/index
DEPLOY_PATH_TARO=

# --------------------------------------------
# 后端配置 (ExpressBackendSkeleton)
# --------------------------------------------
# 注意: 后端的环境变量配置在 ExpressBackendSkeleton/.env 文件中
# 请参考 ExpressBackendSkeleton/.env.example
# 
# 后端会优先读取根目录的 .env 文件中的公共环境变量（如 LOG_LEVEL、DB_LOGGING）
# 然后读取 ExpressBackendSkeleton/.env 文件中的特定配置

# --------------------------------------------
# 前端配置 (VueAdminSkeleton)
# --------------------------------------------
# 注意: 前端的环境变量配置在 VueAdminSkeleton/.env 文件中
# 请参考 VueAdminSkeleton/.env.example
# 
# 前端使用 Vite，环境变量需要以 VITE_ 开头
VITE_LOG_LEVEL=debug

# --------------------------------------------
# Taro 配置 (TaroFrontendSkeleton)
# --------------------------------------------
# 注意: Taro 的环境变量配置在 TaroFrontendSkeleton/.env 文件中
# 请参考 TaroFrontendSkeleton/.env.example
# 
# Taro 环境变量需要以 TARO_APP_ 开头
